# Rehearsal: `hyprws` onto `v0.0.37-nightly.20260829.1224`

## Header

- Source: `origin/hyprws@33873f66f1c50c8960050cc7e8c946ade004cb57`
- Target: `v0.0.37-nightly.20260829.1224@c0e09f323ac9f6bf4b9119cbad841db3379588d6`
- `expected_old`: `33873f66f1c50c8960050cc7e8c946ade004cb57`
- Shared base: `2bc9e8ef6ad57a2513e4552e8a0b21d53db859e6`
- Rehearsal branch: `rehearse/v0.0.37-nightly.20260829.1224`
- Rehearsed product head: `c5207dfbc355ab0a95f60f3648b4a9300a4fe350`
- Stack size: `128` fork commits
- Human sanity: donjor 2026-08-30

The blocked report is `RSI-Software/t3code-hyprws#212`. This target carries the Expo SDK 57 upgrade
`3e6ab36f6ed303f95f210971f56cc8642214c6fc` that blocked the auto-rebase bot.

The first replay produced `a5b2a4f33c3682d34fa76e8980b78a08ae7bd996` with 126 fork commits and stopped
on a failed `fork:scan`. Two repair commits then took the stack to 128 and the product head above. The
record commit makes the pushed head 129 commits above the target; `Rehearsed product head` and
`Stack size` describe the checked 128-commit product stack, because the record commit cannot name its
own SHA.

## Conflicts

| Fork commit and exact subject                                                              | Domain           | File                                             | Hunks | Class      | What upstream changed                                                        | Resolution                                                                                                   | Agent-safe?          |
| ------------------------------------------------------------------------------------------ | ---------------- | ------------------------------------------------ | ----: | ---------- | ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | -------------------- |
| `1a1771d83 fix(desktop): isolate project preview and drafts`                               | project-windows  | `apps/web/src/composerDraftStore.test.ts`        |     1 | mechanical | Added generic file-attachment types, helpers, and tests in `bcb855a63`.      | Kept `resolveComposerDraftStorageKey` and the project-identity tests; took the upstream imports beside them. | yes                  |
| `8c5b3661d feat(web): add rich Markdown editing (#28)`                                     | markdown-editing | `pnpm-lock.yaml`                                 |     1 | generated  | Brought the whole Expo SDK 57 dependency graph and target package snapshots. | Started from the target lockfile and ran `vp i` against the combined manifests, per the mobile-upgrade rule. | yes, generated state |
| `616498943 feat(issues): add GitHub Issues surface scoped to project windows`              | github-issues    | `apps/web/src/components/ChatView.tsx`           |     1 | mechanical | Added a file-attachment capability and maximum-byte lookup.                  | Kept both capability blocks; they are independent and both required.                                         | yes                  |
| `077fefba8 fix(web): returning to a thread focuses the composer, not its terminal`         | upstream-fixes   | `apps/web/src/components/ChatView.logic.ts`      |     1 | mechanical | Added `shoulderTabReserve` for composer spacing.                             | Kept `nextTerminalFocusRequestId` and `shouldAutoFocusComposerOnThreadChange` beside it.                     | yes                  |
| `077fefba8 fix(web): returning to a thread focuses the composer, not its terminal`         | upstream-fixes   | `apps/web/src/components/ChatView.logic.test.ts` |     1 | mechanical | Added the shoulder-tab reserve test.                                         | Kept both test groups; no assertion was weakened.                                                            | yes                  |
| `8c069bd8b fix(web): upload media in pull request descriptions`                            | upstream-fixes   | `apps/web/src/lib/attachmentUploadQueue.ts`      |     3 | seam-moved | Generalized the image queue to all files with a typed claim and 50 MB model. | Attached the fork transport at upstream's new `runAttachmentUploadCycle` seam with `contentType: mimeType`.  | reviewed, see below  |
| `7c43fbe94 docs(fork): fork sync runbook for the auto-rebase model`                        | fork-meta        | `docs/internals/glossary.md`                     |     1 | mechanical | Added Appearance terms and references for environment themes.                | Kept both sections; the fork-sync reference moved to `[27]` so upstream's `[25]` and `[26]` stay intact.     | yes                  |
| `f8de2fb7d fix(contracts): released clients decode threads from a worktrunk server (#233)` | worktrunk-hooks  | `packages/contracts/src/settings.ts`             |     1 | mechanical | Added `defaultTheme` and `defaultThemeSetAt`.                                | Kept `WireThreadEnvMode` and `defaultThreadEnvModeFork` beside the theme settings.                           | yes                  |

Conflict-file class counts: 6 mechanical, 1 seam-moved, 1 generated, 0 human-only.

The seam-moved row is the only behavioural resolution in this wave. It moved the fork's shared
`uploadFileBytes` transport onto upstream's new attachment cycle. The pull-request Summary publication
path was not moved: `apps/web/src/lib/pullRequestAttachmentUpload.ts` still calls `uploadFileBytes`
directly through `createAttachmentUploadUrl` and `uploadAttachment`, so the fork-owned `gh-image`
route is unchanged by this rebase.

## Retire signals

Orientation raised 21 retire signals. All 21 resolved to **keep**, and the independent review upheld
every one. No whole fork commit was retired at this tag. Each keep reason is durable under **Kept** in
[`docs/internals/fork-delta.md`](../../internals/fork-delta.md), reviewed at
`v0.0.37-nightly.20260829.1224`.

The recurring reading is that upstream generalized shared plumbing in this window: file attachments,
composer spacing, environment themes, keybinding settings. None of that supplies a project-window
shell, a GitHub Issues surface, Worktrunk lifecycle hooks, a managed zmux binding, manual thread
ordering, or a Rich/Source Markdown editor.

## Silent seams

Orientation predicted 28 clean automerges. The two that could have changed behavior silently were
checked directly; the rest are covered by the focused suites in Verification.

| Seam                                                                  | Risk                                                                     | Check                                                             | Result                                                                                   |
| --------------------------------------------------------------------- | ------------------------------------------------------------------------ | ----------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| `apps/web/src/components/githubIssue/GitHubIssueDetailPanel.test.tsx` | The hand-built draft fixture predates upstream's required `files` field. | `apps/web` typecheck and the panel's own suite.                   | **Caught a real break.** TS2741 plus an `undefined.length` failure; repaired, see below. |
| `packages/client-runtime/src/state/pullRequests.ts`                   | Pull-request media state could regress beside upstream attachments.      | Focused client-runtime and web pull-request attachment suites.    | Passed; the `gh-image` publication path is untouched by the rebase.                      |
| `apps/server/src/server.ts` service composition                       | Issues, workspace files, Worktrunk, and zmux layers could be dropped.    | Server typecheck and focused server suites for each named domain. | Passed; all four domains compose and their suites are green.                             |

## Verification

Rehearsal checks on the first 126-commit replay:

- `vp i` before and during the replay: **pass** — the Expo SDK 57 graph and the fork Markdown
  dependencies coexist, and repeated install left the tree clean.
- `node scripts/fork-preflight.ts`: **pass** — 6 preconditions met.
- `node scripts/fork-upstream-watch.ts --target v0.0.37-nightly.20260829.1224`: **pass** — read-only
  sweep, `5 open: 1 uncited, 3 waiting, 1 ready`.
- `node scripts/fork-orient.ts --target v0.0.37-nightly.20260829.1224`: **pass** — 8 conflicting
  files, 28 automerged overlaps, 21 retire signals.
- `git rebase v0.0.37-nightly.20260829.1224`: completed after 8 conflict-file resolutions across 7
  fork commits, with all subjects in order and all `Fork-*` trailers preserved.
- `vp run fork:delta --check`: **pass** — 126 fork commits tagged.
- `git diff --check v0.0.37-nightly.20260829.1224..HEAD`: **pass**.
- `vp run fork:scan --head origin/hyprws --target v0.0.37-nightly.20260829.1224`: **fail** — 12
  rebase-scan gaps. The rehearsal stopped here as its brief required.

Two repairs then landed on the rehearsal branch:

- `9f9565dd3 test(web): align issue handoff draft fixture` adds `files: []` to the hand-built
  `ComposerThreadDraftState` in `GitHubIssueDetailPanel.test.tsx`, restoring the shape production
  hydration produces.
- `c5207dfbc docs(fork): cover v0.0.37 rebase seams` adds all 12 missing `Rebase scan` rows:
  three project-windows paths, two markdown-editing paths, one fork-meta path, one
  `apps/server/src/server.ts` row each in workspace-files, worktrunk-hooks, and zmux-estate, and one
  `packages/client-runtime/package.json` row each in custom-agents, github-issues, and
  thread-ordering.

Apply checks on the repaired 128-commit head:

- `vp run fork:scan --head origin/hyprws --target v0.0.37-nightly.20260829.1224`: **pass** — 12
  domain scans, every domain `0 gap(s)`.
- `vp run fork:delta --check`: **pass** — 128 fork commits tagged.
- `git diff --check v0.0.37-nightly.20260829.1224..HEAD`: **pass**; worktree clean.
- `vp fmt --check apps/web packages/contracts apps/mobile docs/internals/fork-delta.md pnpm-lock.yaml`:
  **pass** — 1557 files.
- `vp lint --report-unused-disable-directives apps/web packages/contracts apps/mobile`: **pass** with
  pre-existing warnings and no errors.
- Web, contracts, mobile, desktop, server, client-runtime, and shared typechecks: **pass**.
- `apps/web` focused suites, 19 files: **pass** — 446 tests. Covers project draft isolation, rich
  Markdown, Issues hand-off and routing, terminal focus, and both pull-request attachment seams.
- `apps/server` focused suites, 8 files: **pass** — 238 tests, run with `GIT_CONFIG_GLOBAL=/dev/null`.
- `packages/contracts` focused suites, 4 files: **pass** — 111 tests, including released-client
  Worktrunk decoding.
- `apps/desktop` focused suites, 2 files: **pass** — 43 tests.
- `packages/shared` and `packages/client-runtime` focused suites: **pass** — 8 and 3 tests.

An independent review of the replay agreed with all 8 conflict-file resolutions and all 21 keep
verdicts, and established that the 12 scan gaps were bookkeeping-only: each gap path's fork net delta
was numstat-identical before and after replay.

Two defects found while gating are filed rather than worked around:

- `RSI-Software/t3code-hyprws#262`: `fork:scan` and `fork:orient` cannot see a fork-only file broken by
  an upstream type change, which is exactly how the draft fixture break reached the gate.
- `RSI-Software/t3code-hyprws#263`: `GitManager.test.ts` inherits an ambient global `core.hooksPath`,
  so it fails on a clean tree and reads as fork breakage during a rebase gate.

The server suites above were run with `GIT_CONFIG_GLOBAL=/dev/null` for that reason. No global config
was changed.

## Grounding

Not grounded before apply. The human accepted the gap and directed the landing on 2026-08-30.

Seven claims were raised by the rehearsal and remain open:

- Returning to a thread focuses the composer, while an explicit terminal action still focuses the
  terminal.
- The GitHub Issues list, detail panel, and `Work on this issue` action work in a project window
  without overwriting an invested draft.
- Pull-request Summary media publishes through the retained `gh-image` path.
- Project-window draft persistence stays isolated while generic file attachments survive reload.
- Rich and Source Markdown still load after the Expo 57 lockfile regeneration, with frontmatter
  preserved and MDX still outside Rich editing.
- A released two-value mobile client decodes threads from a server whose default is `worktrunk`.
- The Expo SDK 57 mobile builds start without a header or composer regression.

The two claims closest to this wave's only behavioural resolution are covered statically. The
pull-request Summary publication path was never moved by the rebase, and the Issues draft-preservation
behavior is unit-proved in both directions by the repaired panel suite.

No agent lane can drive an Electron app over CDP: both `peer-worker` families refuse raw CDP by shared
doctrine, and `chrome-grounder` speaks only to managed Chrome. That gap is filed as
`RSI-Software/donjor-skills#398`. Grounding is the human's, after apply.

## Recommendation

`land`.

The 128-commit product stack is checked, every conflict resolution and keep verdict survived an
independent review, all automated gates pass, and the exact lease is recorded. Grounding is deferred
by an explicit human decision, not by omission.
