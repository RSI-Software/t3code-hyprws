# Rehearsal: `hyprws` onto `v0.0.37-nightly.20260830.1226`

## Header

- Source: `origin/hyprws@07795268a9719be56e30cea814bbc62a198bb397`
- Target: `v0.0.37-nightly.20260830.1226@8dcb96314c976899e4df6951fb9af03131c2a46f`
- `expected_old`: `07795268a9719be56e30cea814bbc62a198bb397`
- Rehearsal branch: `rehearse/v0.0.37-nightly.20260830.1226`
- Rebased head: `5d64f95b79c2b15908ac161e7f429587f2924a65`
- Stack size: `135` fork commits
- Human sanity: donjor 2026-08-30

The shared base is `c0e09f323ac9f6bf4b9119cbad841db3379588d6`.

## Conflicts

None.

`git rebase v0.0.37-nightly.20260830.1226` replayed all 135 commits and stopped zero times.
Evidence that nothing was silently dropped, read at the gate 2 head
`5d64f95b79c2b15908ac161e7f429587f2924a65`:

- `git rev-list --count v0.0.37-nightly.20260830.1226..HEAD` is `135`, equal to the pre-rebase stack size.
- Every commit subject is byte-identical to its pre-rebase subject, in the same order.
- Every full commit message, including every `Fork-Domain` and `Fork-Tier` trailer, is byte-identical across all 135 commits.
- Nothing was skipped, squashed, reordered, or reworded.
- `git config rerere.enabled` is `true`; because the rebase stopped zero times, rerere proposed or replayed no conflict resolution.

## Automerged overlap review

The orientation report predicted two overlapping files. Both were reviewed against the shared-base,
published-fork, target, and rebased blobs.

| File             | Fork changed it | Upstream changed it | Rebased result | Reading                                                                                                                                                                         |
| ---------------- | --------------- | ------------------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `AGENTS.md`      | yes             | yes                 | merge of both  | Upstream's new meaningful-test guidance is present; all 29 lines of fork direction remain present above it.                                                                     |
| `pnpm-lock.yaml` | yes             | yes                 | merge of both  | Upstream's Expo/Uniwind lock changes and the fork's `smol-toml` and Milkdown dependency graph all survive; the post-rebase install and the server, web, and mobile checks pass. |

For `AGENTS.md`, the upstream delta is one insertion and `git diff
v0.0.37-nightly.20260830.1226..HEAD` remains exactly the fork's 29-line direction block. For
`pnpm-lock.yaml`, `git diff origin/hyprws..HEAD` has the same 124 insertions and 222 deletions as the
upstream delta, while `git diff v0.0.37-nightly.20260830.1226..HEAD` retains the fork's 1,466
insertions and 38 deletions. Upstream changed only `apps/mobile/package.json`; the fork dependency
manifests remain in `apps/server/package.json` and `apps/web/package.json`.

## Fork commits

| Fork commit and exact subject                             | Domain           | Class summary    | Action                                                                                                                                                                                                                 | Grounding claim                                                                                        |
| --------------------------------------------------------- | ---------------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| `6cd70962bb` `feat(web): add rich Markdown editing (#28)` | markdown-editing | retire-candidate | keep — no content conflict; the report flagged weak lockfile overlap, but upstream changed Expo/Uniwind mobile dependencies rather than the Rich/Source editor seam; the carried-forward `Kept` decision was confirmed | n/a — no new product grounding claim; this target's overlap is confined to the mobile dependency graph |

The other 134 rehearsed commits replayed with no conflict, no new decision, and no grounding claim.
Together with the row above, they reconcile with the header's stack size of 135.

## Silent seams

None. The server typecheck emitted 26 Effect language-service suggestions, but none of their files
changed upstream between the shared base and this target; web and mobile typechecks emitted no
diagnostics. No finding belongs to this rebase seam.

## Verification

- `node scripts/fork-preflight.ts`: **pass** — 6 preconditions met after `vp i` restored the missing checkout dependencies.
- `node scripts/fork-upstream-watch.ts`: **pass** — 6 open watches: 3 waiting, 2 uncited, 1 ready.
- `node scripts/fork-orient.ts --target v0.0.37-nightly.20260830.1226`: **pass** — target and mirror resolved; 2 automerged overlaps and 1 retire candidate reported.
- `wt switch --create rehearse/v0.0.37-nightly.20260830.1226 --base origin/hyprws`: **pass**.
- `vp i` before rebase: **pass**.
- `git rebase v0.0.37-nightly.20260830.1226`: **pass** — 135/135 commits replayed with zero stops.
- `vp i --frozen-lockfile` after rebase: **pass** — the target changed `pnpm-lock.yaml`; its exact 2,136-entry lockfile passed supply-chain policy without a source-tree change.
- `vp run fork:scan --head origin/hyprws --target v0.0.37-nightly.20260830.1226`: **pass** — 12 domain scans cover every shared file; no gaps.
- `vp run fork:delta --check`: **pass** — 135 fork commits tagged.
- `vp run --filter t3 typecheck`: **pass** — no errors; 26 unrelated Effect suggestions are classified under Silent seams.
- `vp run --filter @t3tools/web typecheck`: **pass** — no diagnostics.
- `vp run --filter @t3tools/mobile typecheck`: **pass** — no diagnostics.
- `vp test run apps/server/src/provider/Drivers/CodexAgents.test.ts apps/web/src/components/files/FilePreviewPanel.test.ts apps/web/src/components/files/markdownPipeline.test.ts apps/web/src/markdown-links.test.ts apps/mobile/src/lib/uniwind-dev-refresh.test.ts apps/mobile/scripts/generate-uniwind-themes.test.ts`: **pass** — 6 files, 74 tests.

No assertion was weakened or skipped. Repo-wide checks were not substituted for the targeted set.

## Grounding

None. The human explicitly waived new browser proof for this clean nightly rebase; the triggered
nightly build owns release-level product coverage. The keep decision is structural: the upstream
side of the only dependency overlap is confined to Expo/Uniwind mobile dependencies and does not
touch the fork's Rich/Source editor seam.

Recommendation: land.
