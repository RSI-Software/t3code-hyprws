# Rehearsal: `hyprws` onto `v0.0.37-nightly.20260829.1217`

## Header

- Source: `origin/hyprws@500d730a8f1de38d8450f012036db28993a3eb89`
- Target: `v0.0.37-nightly.20260829.1217@2bc9e8ef6ad57a2513e4552e8a0b21d53db859e6`
- `expected_old`: `500d730a8f1de38d8450f012036db28993a3eb89`
- Shared base: `8fc7f22941969d7a2c06929dc29aa86ae1b02f33`
- Rehearsal branch: `rehearse/v0.0.37-nightly.20260829.1217`
- Rehearsed product head: `d15fc6f133df89804951cdb42df5d41a6413ed36`
- Stack size: `125` fork commits
- Human sanity: donjor 2026-08-30

The original 124-commit rehearsal started from
`a6df51e15ba4a72489785436bf3af5fe928361a1` and produced
`43c59db385b463ce80f27450467ca01f852fd872`. Before apply, trunk advanced by one commit:
`500d730a8f1de38d8450f012036db28993a3eb89 fix(web): make sidebar thread ordering direct (#246)`.
That commit was cherry-picked without conflict at the end of the rehearsed stack, where a fresh
rebase would replay it. Its full message and `Fork-Domain`/`Fork-Tier` trailers are byte-identical to
the source commit. The blocked report is `RSI-Software/t3code-hyprws#212`.

The record commit makes the pushed head 126 commits above the target. `Rehearsed product head` and
`Stack size` above describe the checked 125-commit product stack; the record commit cannot name its
own SHA.

## Conflicts

| Fork commit and exact subject                                        | Domain           | File             | Hunks | Class            | Effort | What upstream changed                                                                                                   | Resolution                                                                                                                                                                     | Agent-safe?                                                                   |
| -------------------------------------------------------------------- | ---------------- | ---------------- | ----: | ---------------- | ------ | ----------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------- |
| `a01c41ef2 feat(web): add rich Markdown editing (#28)` → `8c5b3661d` | markdown-editing | `pnpm-lock.yaml` |     1 | retire-candidate | M      | Removed the Messages Glass Lab experiment, updated Expo/React peer snapshots, and changed package versions to `0.0.36`. | Started from the target lockfile and ran `vp i` against the combined manifests, preserving Milkdown, frontmatter, `happy-dom`, and the earlier custom-agent `smol-toml` graph. | no — human keep/retire sign-off required and received from donjor 2026-08-30. |

Conflict-file class counts: 0 mechanical, 0 seam-moved, 1 retire-candidate, 0 human.

## Automerged overlap review

| File                       | Fork changed it                                                 | Upstream changed it                      | Rehearsed result                                        | Reading                                                                                            |
| -------------------------- | --------------------------------------------------------------- | ---------------------------------------- | ------------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| `apps/server/package.json` | Added `smol-toml` for custom main-thread agents.                | Changed the package version to `0.0.36`. | Version is `0.0.36`; `smol-toml` remains `^1.7.0`.      | Both independent edits survived; server typecheck and focused Codex agent/adapter tests passed.    |
| `apps/web/package.json`    | Added Milkdown, frontmatter, and rich-editor test dependencies. | Changed the package version to `0.0.36`. | Version is `0.0.36`; all four fork dependencies remain. | Both independent edits survived; stable install, web typecheck, and focused Markdown tests passed. |

No other overlap was a retirement candidate in this target window.

## Fork commits

| Fork commit and exact subject                                    | Domain           | Class summary                      | Action                                                                                                                                                                                                                                         | Grounding claim                                                                                                                     |
| ---------------------------------------------------------------- | ---------------- | ---------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| `8c5b3661d feat(web): add rich Markdown editing (#28)`           | markdown-editing | retire-candidate lockfile conflict | **keep** — the target removes only the unrelated Messages Glass Lab experiment and bumps versions. It supplies no rich Markdown editor, safe frontmatter round trips, or Rich/Source surface, so there is no upstream replacement at this tag. | Rich and Source load; a Rich save survives reopen with YAML frontmatter; MDX and truncated files remain outside editable Rich mode. |
| `d15fc6f13 fix(web): make sidebar thread ordering direct (#246)` | thread-ordering  | trunk drift                        | keep — cherry-picked onto the rehearsed stack at apply after the source trunk advanced; full message and trailers are byte-identical and the cherry-pick had no conflict.                                                                      | n/a — no product grounding claim for this target conflict.                                                                          |

The other 123 product commits need no decision row for this sync. They plus the two rows above
reconcile with the product head's stack size of 125. The keep decision is durable under **Kept** in
[`docs/internals/fork-delta.md`](../../internals/fork-delta.md).

## Silent seams

| Seam                                                 | Risk                                                                     | Check                                                                                | Result                                                            |
| ---------------------------------------------------- | ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------ | ----------------------------------------------------------------- |
| `apps/server/package.json` release-version automerge | The version bump could displace the custom-agent TOML parser dependency. | Server typecheck, focused Codex agent/adapter tests, and final manifest inspection.  | Passed; `smol-toml` is present and the version is `0.0.36`.       |
| `apps/web/package.json` release-version automerge    | The version bump could displace Milkdown/frontmatter/test dependencies.  | Stable `vp i`, web typecheck, focused Markdown tests, and final manifest inspection. | Passed; all dependencies are present and the version is `0.0.36`. |

## Verification

Rehearsal checks on the 124-commit rebased head:

- `vp i`: **pass** — the combined lockfile was stable on rerun.
- `vp run fork:scan --head origin/hyprws --target v0.0.37-nightly.20260829.1217`: **pass** — 12 domains, 0 gaps.
- `vp run fork:delta --check`: **pass** — 124 fork commits tagged.
- `vp fmt --check apps/desktop apps/server apps/web packages/contracts pnpm-lock.yaml`: **pass**.
- `vp lint --report-unused-disable-directives apps/desktop apps/server apps/web packages/contracts`: **pass** with pre-existing warnings and no errors.
- Web, server, desktop, and contracts targeted typechecks: **pass**.
- `(cd apps/web && vp test run --project unit src/components/files/FilePreviewPanel.test.ts src/components/files/markdownPipeline.test.ts src/markdown-links.test.ts)`: **pass** — 3 files, 62 tests.
- `vp test run apps/server/src/provider/Drivers/CodexAgents.test.ts apps/server/src/provider/Layers/CodexAdapter.test.ts`: **pass** — 2 files, 37 tests.
- `git diff --check v0.0.37-nightly.20260829.1217..HEAD`: **pass**.
- `node scripts/fork-preflight.ts`: **pass** — 6 preconditions met.
- Pre-/post-rebase subject, order, and full-message comparison: **pass** for all 124 replayed commits.

Apply checks after the one-commit trunk drift was added:

- Full-message comparison of `500d730a8` and `d15fc6f13`: **pass** — byte-identical, including `Fork-*` trailers.
- `vp run fork:delta --check`: **pass** — 125 fork commits tagged.
- `vp run fork:scan --head origin/hyprws --target v0.0.37-nightly.20260829.1217`: **pass** — all 12 domain scans cover their shared files.
- `node scripts/fork-preflight.ts`: **pass** — 6 preconditions met; freshly fetched `origin/hyprws` is the exact `expected_old`.
- `vp fmt --check apps/web docs/internals/fork-delta.md docs/user/thread-sidebar.md`: **pass**.
- `vp lint --report-unused-disable-directives apps/web`: **pass** with pre-existing warnings and no errors.
- `vp run --filter @t3tools/web typecheck`: **pass**.

The unpinned advisory `vp run fork:scan` also ran and reported 12 gaps against live
`upstream/main`; the required target-pinned Gate 3 scan above passed. No assertion was weakened or
skipped, and no replayed commit was reworded.

## Grounding

Grounded 2026-08-29 in the running Electron app at
`43c59db385b463ce80f27450467ca01f852fd872`; accepted by donjor 2026-08-30 before apply.

- Grounding claim: the project was added through the app's own UI | **grounded**.
- Grounding claim: `showcase.md` offers working Rich and Source modes | **grounded** — both rendered without a module-resolution or Milkdown error.
- Grounding claim: a Rich edit saves and survives reopen with YAML frontmatter intact | **grounded**.
- Grounding claim: Source mode shows the same saved bytes, including frontmatter | **grounded**.
- Grounding claim: MDX offers no editable Rich mode | **grounded**.
- Grounding claim: a file reported as truncated is read-only and has no mounted rich editor | **grounded**.
- Grounding claim: a workspace file link stays in the project window, an HTTP link does not hijack it, and the hub remains open | **grounded**.

The run observed no module-resolution error, no Milkdown error, and no editor exception. A
ProseMirror CSS recommendation and unrelated custom-scheme cookie-write exceptions did not affect
the proved behavior.

## Recommendation

`land`.

The conflict has a signed-off keep decision, Electron grounding covers all seven claims, the
125-commit product stack is checked, and the exact trunk-drift lease is recorded.
