# Rehearsal: `hyprws` onto `v0.0.35`

## Header

- Source: `origin/hyprws@73a8d4ec3ce91fa8df62f76bed1fdb0882db524f`
- Target: `v0.0.35@f925d639421844f02b3166d29281905dbba6d529`
- `expected_old`: `73a8d4ec3ce91fa8df62f76bed1fdb0882db524f`
- Rehearsal branch: `rehearse/v0.0.35`
- Rebased head: `06f5d9d6dd82eb3717f18b5d90555aabfde18456`
- Stack size: `85` fork commits
- Human sanity: donjor 2026-08-28

Shared base with the target is `v0.0.34@badae6a5cc8325dcd5a145bea6f7b8ac692818a1`, so this rehearsal
replays the fork stack across exactly one upstream release.

The gate 2 rebase produced `66a240c6c61982d0dd993872eaacf8127e0d48a8` with 85 commits. Gate 4 then
retired `fix(web): scope markdown actions to thread environment` and dropped it with
`git rebase --onto ab81c4e05f^ ab81c4e05f rehearse/v0.0.35`, which replayed the 83 commits above it
without a stop, leaving 84. Gate 5 then found the published head had moved and incorporated the one
new fork commit, `fix(desktop): honor embedded browser wheel zoom (#169)`, onto the rehearsed stack;
see **Post-rehearsal drift**. The head and stack size recorded here are the post-drop, post-drift
values.

They are not, quite, what the lease push publishes. Gate 4 commits this record and the ledger onto the
rehearsal branch as `docs(fork): record v0.0.35 rehearsal`, an 86th fork commit in the `fork-meta`
domain, so the pushed head is that commit and `fork:delta --check` reports `ok: 86 fork commits
tagged` against it. `Rebased head` and `Stack size` above describe the rehearsed stack, which is the
thing the conflicts, commit table, and checks are evidence about. The record commit cannot name its
own SHA, so it is identified by subject.

## Conflicts

None.

`git rebase v0.0.35` replayed all 85 commits and stopped zero times. There is no conflicted
(fork commit, file) pair, so this record carries no conflict table.

Evidence that nothing was silently dropped in the replay. All of it was read at the gate 2 head
`66a240c6c6`, before the gate 4 retirement drop, so the counts here are 85 by design:

- `git rev-list --count v0.0.35..HEAD` is `85`, equal to the pre-rebase stack size.
- Every commit subject is byte-identical to its pre-rebase subject, in the same order.
- Every full commit message, including every `Fork-Domain` and `Fork-Tier` trailer, is
  byte-identical to its pre-rebase message across all 85 commits.
- Nothing was skipped, squashed, reordered, or reworded.

`git config rerere.enabled` was unset in this checkout and no `rr-cache` exists, so no rerere
resolution was proposed or replayed. Nothing in this rehearsal came from a cached resolution.

## Automerged overlap review

The orientation report predicted six overlapping files that Git would automerge. A clean automerge is
not evidence that fork behaviour survived, so each file is classified below by comparing the rebased
blob against the shared-base, fork, and upstream blobs.

| File                                            | Fork changed it | Upstream changed it | Rebased result | Reading                                                             |
| ----------------------------------------------- | --------------- | ------------------- | -------------- | ------------------------------------------------------------------- |
| `apps/web/src/components/Sidebar.logic.ts`      | yes             | yes                 | merge of both  | Fork helper retained beside upstream's new sort anchor.             |
| `apps/web/src/components/Sidebar.logic.test.ts` | yes             | yes                 | merge of both  | Upstream added two cases; fork cases untouched.                     |
| `packages/contracts/src/orchestration.ts`       | yes             | yes                 | merge of both  | Upstream added `unsettledAt` to two structs; fork fields untouched. |
| `apps/server/package.json`                      | yes             | yes                 | merge of both  | Upstream version bump only.                                         |
| `apps/web/package.json`                         | yes             | yes                 | merge of both  | Upstream version bump only.                                         |
| `docs/user/thread-sidebar.md`                   | yes             | yes                 | merge of both  | Upstream added one paragraph; fork paragraphs untouched.            |

For every one of the six, `git diff origin/hyprws..HEAD -- <file>` contains upstream-only content: the
`unsettledAt` re-entry anchor for the active-list sort, its two tests, its user-doc paragraph, and the
`0.0.33` to `0.0.34` package version bumps. No fork hunk was rewritten or lost.

`apps/desktop/package.json` and `packages/contracts/package.json` also changed upstream, but the fork
never touched them, so they resolve to upstream exactly and are not overlap.

Seam check on the one file where fork and upstream logic meet,
`apps/web/src/components/Sidebar.logic.ts`:

- Upstream replaced the `sortThreadsForSidebar` comparator's `parseTimestampMs(createdAt)` with
  `activeThreadAnchorTimestampMs`, widened the generic to accept `unsettledAt`, and rewrote the
  block comment above it. The rebased file carries that upstream change intact.
- The fork's `isProjectInSidebarScope` export and its `ScopedProjectRef` import are present at the
  rebased head, unchanged, and are the only fork delta remaining in this file
  (`git diff v0.0.35..HEAD` on it is exactly that helper plus its import).
- Its call sites are identical before and after the rebase: `Sidebar.tsx:1735`,
  `LegacySidebar.tsx:3098` and `:3109`, plus three cases in `Sidebar.logic.test.ts`. The helper is
  still wired, not orphaned.

That is a structural observation from the diff and the call graph. It is not a product claim; whether
scoped sidebars behave correctly is a grounding claim below, settled at gate 4.

## Fork commits

One row per decision, claim, or change of stack membership, keyed by exact subject. Rehearsal SHAs are
from `rehearse/v0.0.35` and are not the identity; the subject is.

Three commits earn a row. `ab81c4e05f` carries its pre-drop SHA because it was retired at gate 4 and
dropped from the stack; every other SHA here is a post-drop value, because dropping that commit
rewrote the 83 above it. `06f5d9d6dd` was published on `origin/hyprws` after the rehearsal started and
was incorporated at gate 5, so its SHA is the post-incorporation value; see **Post-rehearsal drift**.

| Fork commit and exact subject                                                    | Domain          | Class summary                                                            | Action                                                                                                                     | Grounding claim                                                                                                                     |
| -------------------------------------------------------------------------------- | --------------- | ------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| `ab81c4e05f` (pre-drop) `fix(web): scope markdown actions to thread environment` | project-windows | no content conflict; carried-forward retire-candidate                    | retire — dropped from the stack at gate 4; `Retired` row added to `docs/internals/fork-delta.md`                           | n/a — no product grounding claim                                                                                                    |
| `89ef7542d6` `refactor(web): add physical project sidebar scope`                 | project-windows | no content conflict; report-flagged retire-candidate                     | keep — `Kept` row added to `docs/internals/fork-delta.md`; upstream refactored the same file without replacing the scoping | Grounded 2026-08-28: a project-scoped window's sidebar lists only that project's threads; the hub window still lists every project. |
| `06f5d9d6dd` `fix(desktop): honor embedded browser wheel zoom (#169)`            | upstream-fixes  | no content conflict; entered the stack at gate 5 as post-rehearsal drift | keep — cherry-picked onto the rehearsed stack; message and `git patch-id --stable` byte-identical to the published commit  | n/a — no product grounding claim                                                                                                    |

The other 83 rehearsed commits replayed with no content conflict, no decision, and no grounding claim,
and the schema does not repeat the stack. Those 83 plus the two rows above that are still in the stack
reconcile with the header's `Stack size` of 85. The rehearsal walked 86 commits: those 85 plus
`fix(web): scope markdown actions to thread environment`, which the retirement dropped.

## Domain summary and rebase scan

Conflict columns are all zero because the rebase never stopped. The rebase-scan column records the
gate 3 walk: every row of every domain's `### Rebase scan` table in
[`docs/internals/fork-delta.md`](../../internals/fork-delta.md) was checked against the 32 files
upstream changed in `v0.0.34..v0.0.35`. A row "fires" when upstream touched its path.

| Domain           | Commits | Mechanical | Seam-moved | Retire-candidate | Human | Rebase scan                                                         |
| ---------------- | ------: | ---------: | ---------: | ---------------: | ----: | ------------------------------------------------------------------- |
| project-windows  |      28 |          0 |          0 |                0 |     0 | walked, 19 rows, 0 fired; scan under-covers this domain (see below) |
| fork-meta        |      27 |          0 |          0 |                0 |     0 | walked, 5 rows, 0 fired                                             |
| upstream-fixes   |      12 |          0 |          0 |                0 |     0 | walked, 1 row (per-commit diff), 0 fired                            |
| zmux-estate      |       5 |          0 |          0 |                0 |     0 | walked, 5 rows, 0 fired                                             |
| worktrunk-hooks  |       4 |          0 |          0 |                0 |     0 | walked, 14 rows, 1 fired: `packages/contracts/src/orchestration.ts` |
| github-issues    |       3 |          0 |          0 |                0 |     0 | walked, 19 rows, 0 fired                                            |
| distribution     |       3 |          0 |          0 |                0 |     0 | walked, 5 rows, 1 fired: `.github/workflows/release.yml`            |
| workspace-files  |       1 |          0 |          0 |                0 |     0 | walked, 8 rows, 0 fired                                             |
| markdown-editing |       1 |          0 |          0 |                0 |     0 | walked, 6 rows, 1 fired: `apps/web/package.json`                    |
| custom-agents    |       1 |          0 |          0 |                0 |     0 | walked, 9 rows, 0 fired; scan under-covers this domain (see below)  |
| **Total**        |  **85** |      **0** |      **0** |            **0** | **0** | 91 rows walked, 3 fired                                             |

### Fired rows

**worktrunk-hooks — `packages/contracts/src/orchestration.ts`.** Upstream added
`unsettledAt: Schema.optional(Schema.NullOr(IsoDateTime))` at two places (`OrchestrationThread`, and
the projection struct that mirrors it). The fork's only delta in this file is
`worktrunk: Schema.optional(Schema.Boolean)` on `ThreadTurnStartBootstrapPrepareWorktree`. The two
live in disjoint structs, both are present at the rebased head, and the scan row's stated concern
(`prepareWorktree.worktrunk` on the bootstrap payload) is intact. No fork action.

**distribution — `.github/workflows/release.yml`.** The scan row says to copy Linux build-step
changes into `hyprws-release.yml`. Upstream's change is a nightly-serialisation `concurrency` group
keyed on the `schedule`/`nightly` channel, an off-the-hour cron minute, re-parenting
`relay_public_config` and `build_wsl_node_pty` from `preflight` onto `check_changes`, and dropping a
redundant `vp run --filter @t3tools/web build` step because `t3`'s build task already depends on it.
`hyprws-release.yml` is tag-only with no cron and no nightly channel, has neither of those two jobs,
and never carried the redundant web build step; it goes straight to `vp run dist:desktop:artifact`.
Nothing to copy. No fork action.

**markdown-editing — `apps/web/package.json`.** Upstream's change is the `0.0.34` to `0.0.35`
version bump only. The Milkdown dependency block the row exists to protect is untouched, and
`pnpm-lock.yaml` did not change upstream. No fork action.

### Scan coverage gap

Four of the six automerged overlap files are covered by no scan row in any domain:

| Overlap file                                    | Owning fork commit                                     | Domain          | In a scan table? |
| ----------------------------------------------- | ------------------------------------------------------ | --------------- | ---------------- |
| `apps/web/src/components/Sidebar.logic.ts`      | `refactor(web): add physical project sidebar scope`    | project-windows | no               |
| `apps/web/src/components/Sidebar.logic.test.ts` | `refactor(web): add physical project sidebar scope`    | project-windows | no               |
| `docs/user/thread-sidebar.md`                   | `feat(web): open project windows from hub actions`     | project-windows | no               |
| `apps/server/package.json`                      | `feat(agents): select custom main-thread agents (#31)` | custom-agents   | no               |

The project-windows scan lists `SidebarChrome.tsx` and `windowProjectScope.ts` but not
`Sidebar.logic.ts`, which is where `isProjectInSidebarScope` actually lives and where upstream landed
its sort-anchor change this tag. The custom-agents scan lists source paths but not the
`apps/server/package.json` dependency it added. Walking the scan alone would have missed the one real
fork/upstream seam in this rebase; it was caught by the automerge diff review instead. Adding these
four paths to their domains' scan tables is a fork-delta edit, so it is left for the human at gate 4
rather than made here.

## Verification

Gate 2 and gate 3 commands were run from the rehearsal worktree at
`66a240c6c61982d0dd993872eaacf8127e0d48a8`; gate 4 commands were run at the post-drop head
`aae98a30127fc4f34b2c52804d50b054fc2ab364`.

Gate 2 commands:

- `wt switch --create rehearse/v0.0.35 --base origin/hyprws`: **pass** — worktree at
  `~/donjor/.t3code-hyprws/rehearse-v0.0.35`.
- `vp i` before the rebase: **pass** — 1920 packages, pnpm 11.10.0.
- `git rev-parse origin/hyprws`: **pass** — `9725341c83495aec4ed0fdc4bf9dc498b417d8f3`.
- `git rev-parse v0.0.35^{commit}`: **pass** — `f925d639421844f02b3166d29281905dbba6d529`.
- `git rebase v0.0.35`: **pass** — `Successfully rebased and updated refs/heads/rehearse/v0.0.35`,
  85/85 replayed, zero stops.
- `git rev-list --count v0.0.35..HEAD`: **pass** — `85`.
- `git status --short`: **pass** — clean apart from this untracked record.

Gate 3 ledger check:

- `vp run fork:delta --check` (`node scripts/fork-delta.ts --check`): **pass** in 0.39s —
  `ok: 85 fork commits tagged`. Every rehearsed commit still carries its `Fork-Domain` and
  `Fork-Tier` trailers after the replay.

Gate 3 targeted typechecks, one per package the six overlap files touch:

- `vp run --filter @t3tools/contracts typecheck`: **pass** in 0.72s — `tsgo --noEmit`, no
  diagnostics.
- `vp run --filter @t3tools/web typecheck`: **pass** in 3.45s — `tsgo --noEmit`, no diagnostics.
- `vp run --filter t3 typecheck`: **pass** in 5.37s — `tsgo --noEmit`, exit 0 with 13
  `suggestion TS377019` diagnostics. See **Silent seams**; none are fork-introduced and none are
  errors.

Gate 3 tests beside every touched file and beside every fired scan row:

- `vp test run apps/web/src/components/Sidebar.logic.test.ts packages/contracts/src/orchestration.test.ts`:
  **pass** in 1.21s — 2 files, 156 tests. Covers the one file where fork and upstream logic meet and
  the contract the worktrunk-hooks scan row names.
- `vp test run scripts/fork-delta.test.ts scripts/fork-sync-gate.test.ts scripts/fork-upstream-refs.test.ts scripts/fork-rebase-report.test.ts scripts/fork-rebase-report-artifact.test.ts scripts/fork-upstream-watch.test.ts`:
  **pass** in 1.32s — 6 files, 89 tests. The fork-meta tooling that gates 3 through 5 depend on still
  works against the rebased tree.
- `vp test run apps/server/src/worktrunk/WorktrunkHookRunner.test.ts packages/shared/src/threadEnvMode.test.ts apps/web/src/components/files/markdownPipeline.test.ts`:
  **pass** in 1.09s — 3 files, 14 tests. Consumers of the two fired code-bearing scan rows.

Total: 11 test files, 259 tests, 0 failures. No remediation was needed and no assertion was weakened
or skipped.

Deliberately not run, and why:

- `vp i` after the rebase: **not run** — the gate 2 install was still valid; every typecheck and test
  above resolved its modules and passed, which is the proof that would have justified reinstalling.
- `vp check`, `vp run typecheck` (repo-wide), `vp run build:desktop`: **not run** — gate 3 forbids
  substituting repo-wide checks for targeted ones, and fork CI owns the full suite. Note that
  [the runbook](../fork-sync.md) step 3 still lists these three; that text contradicts the skill's
  gate 3 and should be reconciled.

Gate 4 retirement drop and re-checks:

- `git rebase --onto ab81c4e05f^ ab81c4e05f rehearse/v0.0.35`: **pass** — 83/83 replayed, zero stops,
  `Successfully rebased and updated refs/heads/rehearse/v0.0.35`. New head
  `aae98a30127fc4f34b2c52804d50b054fc2ab364`.
- `git rev-list --count v0.0.35..HEAD`: **pass** — `84`, one fewer than before the drop.
- `git log --oneline v0.0.35..HEAD | grep -c 'scope markdown actions'`: **pass** — `0`. The retired
  subject is gone from the stack, which is what `fork:delta --check` requires of a retired-only row.
- `git diff --stat v0.0.35..HEAD -- apps/web/src/components/ChatMarkdown.test.tsx`: **pass** — empty.
  The test file is byte-identical to upstream `v0.0.35`.
- `git diff v0.0.35..HEAD -- apps/web/src/components/ChatMarkdown.tsx`: **pass with a residual** — the
  retired binding is gone and reads upstream's inlined
  `threadRef?.environmentId ?? explicitEnvironmentId ?? null` again, so the file is byte-identical to
  upstream at that binding. One unrelated fork hunk remains: a comment reworded from
  `A link to a change request` to `A link to a pull request or issue` by
  `feat(issues): add GitHub Issues surface scoped to project windows` in the github-issues domain.
  That is a different fork commit with its own ledger identity and is not part of this retirement.
- `vp run --filter @t3tools/web typecheck`: **pass** — `tsgo --noEmit`, exit 0, no diagnostics.
- `vp test run apps/web/src/components/ChatMarkdown.test.tsx`: **pass** in 1.18s — 1 file, 30 tests.
  The fork's three `resolveChatMarkdownEnvironmentId` cases left with the commit; the 30 remaining are
  upstream's own.
- `node scripts/fork-delta.ts --check` (`vp run fork:delta --check`): **pass** — `ok: 84 fork commits
tagged`, with the `Retired` row present and its subject absent from the stack.
- `node scripts/fork-upstream-refs.ts docs/operations/fork-sync-records/v0.0.35.md`: **pass** —
  `ok: no live upstream references`.
- `node scripts/fork-upstream-refs.ts docs/internals/fork-delta.md`: **pass** —
  `ok: no live upstream references`.

Gate 5 drift incorporation and re-checks, run at the new head
`06f5d9d6dd82eb3717f18b5d90555aabfde18456` with the record commit replayed on top:

- `git fetch origin hyprws`: **pass** — `origin/hyprws` is
  `73a8d4ec3ce91fa8df62f76bed1fdb0882db524f`.
- `git merge-base --is-ancestor 9725341c8 origin/hyprws`: **pass** — the recorded `expected_old` is
  still an ancestor, so the drift is fast-forward.
- `git rev-list --count 9725341c8..origin/hyprws`: **pass** — `1`.
- `git cherry-pick 73a8d4ec3` then `git cherry-pick 05a346cf7`: **pass** — zero conflicts.
- `diff <(git log -1 --format=%B 73a8d4ec3) <(git log -1 --format=%B HEAD~1)`: **pass** — empty. The
  incorporated message and its trailers are byte-identical.
- `git patch-id --stable` on both commits: **pass** — both
  `3b92267bc661ceeb1e40f83146fbb628311f3f61`.
- `git rev-list --count v0.0.35..HEAD~1`: **pass** — `85`, matching the header's stack size.
- `node scripts/fork-delta.ts --check` (`vp run fork:delta --check`): **pass** — `ok: 86 fork commits
tagged`, the 85-commit stack plus this record commit.
- `node scripts/fork-upstream-refs.ts docs/operations/fork-sync-records/v0.0.35.md`: **pass** —
  `ok: no live upstream references`.
- `vp run --filter @t3tools/desktop typecheck`: **pass** — `tsgo --noEmit`, exit 0, no diagnostics.
  Three pre-existing Effect style _suggestions_ in `DesktopBackendPool.test.ts`,
  `DesktopWindowSession.ts`, and `DesktopWslEnvironment.ts`; none in the incorporated files.
- `vp test run apps/desktop/src/preview/Manager.test.ts`: **pass** in 750ms — 1 file, 65 tests.
- `node scripts/fork-sync-gate.ts --tag v0.0.35` (`vp run fork:sync-gate --tag v0.0.35`), re-run
  after the fetch and after the record was amended: **pass** —
  `ready: v0.0.35 apply gate passed at 73a8d4ec3ce91fa8df62f76bed1fdb0882db524f`. The gate compares
  the record's `expected_old` against the local `origin/hyprws` ref and does not fetch, so its
  earlier `ready` at the stale head was not evidence the lease was current. Always fetch first.

## Silent-merge review

Six files automerged; all six are reviewed above under **Automerged overlap review**, and the domain
scan walk above confirms which of them a scan row would have caught.

Only one automerge puts fork and upstream logic in the same construct:
`apps/web/src/components/Sidebar.logic.ts`. Upstream's
`fix(app): un-settled threads return to the top of the list (#8231)` rewrote the
`sortThreadsForSidebar` comparator to anchor on `activeThreadAnchorTimestampMs`; the fork's
`isProjectInSidebarScope` is a separate export with its own call sites. At the rebased head both are
present, upstream's comparator is intact, and the fork helper is still called from `Sidebar.tsx`,
`LegacySidebar.tsx`, and three cases in `Sidebar.logic.test.ts`. `Sidebar.logic.test.ts` passes with
both upstream's new cases and the fork's scope cases in the same file, which is the strongest
structural evidence available without a browser.

The remaining five automerges are non-contending: two package version bumps, one additive schema
field in a struct the fork does not touch, its two tests, and one appended user-doc paragraph.

Nothing was silently dropped, and nothing was silently kept that upstream has superseded, with one
qualification: whether `isProjectInSidebarScope` is still _needed_ is a retirement question, not a
merge question, and it is the open decision at gate 4.

## Silent seams

One class of typecheck-only finding, and it is inherited rather than fork-introduced.

`vp run --filter t3 typecheck` exits 0 but reports 13 `suggestion TS377019`
(`effect(unnecessaryFailYieldableError)`) diagnostics across four files:
`src/orchestration/decider.ts` (8), `src/orchestration/workflowScriptQuery.ts` (3),
`src/provider/Layers/ClaudeAdapter.ts` (1), `src/pullRequest/GitLabPullRequestCli.ts` (1).

Attribution, so this is not mistaken for a rebase seam:

- `decider.ts`, `workflowScriptQuery.ts`, and `GitLabPullRequestCli.ts` have **zero** fork delta at
  the rebased head (`git diff v0.0.35..HEAD` on each is empty). They are upstream files verbatim.
- `ClaudeAdapter.ts` does carry fork delta, but its last fork hunk ends at line 4371 and the reported
  suggestion is at line 4721, inside upstream code that the fork's insertions merely shifted
  downward.

So all 13 are upstream's own suggestions, not errors, and not something this rebase introduced or can
fix. They are recorded here because a reader running the same targeted typecheck will see them and
needs to know they are not a regression.

No adjacent test exposed a seam: all 11 test files and 259 tests passed on the first run.

## Grounding

Both claims were host-owned browser proof and both are now settled. They were grounded on 2026-08-28
against the rehearsal desktop build over CDP, with seeded userdata carrying 21 threads across several
projects. Each records the exact control that was driven and the exact outcome that was observed.

- Grounding settled 2026-08-28: a project-scoped desktop window's sidebar lists only that project's
  threads | a project-scoped desktop window opened from the command palette's **Open project in new
  window** action, beside the hub window | **pass**. The project window's sidebar listed 6 threads,
  all belonging to that window's project, and no thread from any other project; the hub window listed
  all 21. In the hub window the **Filter threads by project** control narrowed the list 21 → 6 for
  that project and restored it 6 → 21 when cleared. Zero console errors on either window. This is the
  product claim behind `refactor(web): add physical project sidebar scope`, and it is what the gate 4
  keep decision rests on: the scoping still works at this tag, after upstream landed a new sort anchor
  in the same file.
- Grounding settled 2026-08-28: un-settling a thread returns it to the top of the active list | the
  thread sidebar in the rehearsal desktop build, with at least one settled thread and several active
  threads | **pass**. **Un-settle thread** moved the row out of **Settled** to index 0 of the active
  list, and **Settle thread** returned it to **Settled**. Zero console errors. This is upstream
  `pingdotgg/t3code#8231` behaviour arriving through the rebase into a file the fork also edits, so
  it is proof that the automerge preserved upstream intent, not only fork intent.

The hub control is a **Filter threads by project** control listing each project, not the
**All projects** / **This project** pair the gate 3 draft of this record predicted. The claim was
written from the fork commit's intent rather than from the shipped UI; the label above is the one
that exists in the build. The expected outcome held either way.

Neither claim could have been settled from the diff, the typecheck, or `Sidebar.logic.test.ts`. The
unit test proves the comparator and the scope predicate in isolation; it does not prove the sidebar
renders what those functions return.

## Post-rehearsal drift

One commit of drift, incorporated at gate 5. `origin/hyprws` was
`9725341c83495aec4ed0fdc4bf9dc498b417d8f3` when the rehearsal started, and the gate 4 record named
that head as `expected_old`. A gate 5 `git fetch origin hyprws` read the published head as
`73a8d4ec3ce91fa8df62f76bed1fdb0882db524f`, with the recorded `expected_old` still an ancestor and
exactly one commit between them: `fix(desktop): honor embedded browser wheel zoom (#169)`, domain
`upstream-fixes`, tier `bugfix`, tagged `Fork-Upstreamable: yes`.

The stale head was a hard stop, not a licence to refresh the lease. The new commit was incorporated
onto the rehearsed stack below the record commit, so the record still sits last:

```bash
git checkout -B rehearse/v0.0.35 aae98a301
git cherry-pick 73a8d4ec3   # -> 06f5d9d6dd82eb3717f18b5d90555aabfde18456
git cherry-pick 05a346cf7   # the gate 4 record commit, replayed on top
```

Both cherry-picks applied with no conflict. The incorporated commit's full message, including its
`Fork-Domain`, `Fork-Tier`, and `Fork-Upstreamable` trailers, is byte-identical to the published
one (`diff` of `git log -1 --format=%B` on both, empty), and `git patch-id --stable` is
`3b92267bc661ceeb1e40f83146fbb628311f3f61` for both, so the content is unchanged as well. It touches
`apps/desktop/src/preview/Manager.ts` and `apps/desktop/src/preview/Manager.test.ts` only, which no
other domain's rebase scan claims, so no automerged-overlap or conflict evidence above is affected.
The header's Source, `expected_old`, rebased head, and stack size, the commit table, and the gate 5
checks under **Verification** are all updated to the post-drift values.

## Carried-forward decision

The `v0.0.34` record left one retire-candidate open,
`fix(web): scope markdown actions to thread environment`. It is resolved at this tag: the human
retired it on 2026-08-28 and it now has a `Retired` row in
[`docs/internals/fork-delta.md`](../../internals/fork-delta.md). It is no longer in the stack.

## Human decisions

Both open decisions were resolved by the human on 2026-08-28 and are recorded, keyed by exact
subject, in [`docs/internals/fork-delta.md`](../../internals/fork-delta.md).

1. **Retire** `fix(web): scope markdown actions to thread environment`. Upstream
   `pingdotgg/t3code#7140` (`082e6ea52`) inlines
   `threadRef?.environmentId ?? explicitEnvironmentId ?? null` at the same binding in
   `apps/web/src/components/ChatMarkdown.tsx`, leaving the fork's `resolveChatMarkdownEnvironmentId`
   a redundant wrapper around upstream's own expression. Dropped from the stack at gate 4; see the
   gate 4 block under **Verification** for the drop and its re-checks.
2. **Keep** `refactor(web): add physical project sidebar scope`. Upstream `pingdotgg/t3code#8231`
   refactored the same `apps/web/src/components/Sidebar.logic.ts` but replaced only the sort anchor,
   not the scoping; the fork's `isProjectInSidebarScope` survived the automerge intact and is still
   the only source of project-window sidebar scoping. Grounded on 2026-08-28; see **Grounding**.

## Recommendation

`land`.

Every gate 3 check passed: `fork:delta --check` green, all three targeted typechecks exit 0 with no
fork-introduced findings, and 259 tests across 11 files pass. The rebase itself is mechanically
clean: zero conflicts, zero silent drops, all trailers intact.

Gate 4 closed both open decisions, dropped the retired commit, and re-ran the affected checks green
at the post-drop head: `fork:delta --check` reports `ok: 84 fork commits tagged`, `@t3tools/web`
typechecks clean, and `ChatMarkdown.test.tsx` passes at 30 upstream tests. Both grounding claims are
settled with observed evidence. Nothing is left open that blocks the lease push.

Two non-blocking follow-ups, since both are edits to
[`docs/internals/fork-delta.md`](../../internals/fork-delta.md) that only a human should make:

- Add `apps/web/src/components/Sidebar.logic.ts`, `apps/web/src/components/Sidebar.logic.test.ts`,
  and `docs/user/thread-sidebar.md` to the project-windows rebase scan, and `apps/server/package.json`
  to the custom-agents rebase scan. This tag's only real seam sat in a file no scan row named.
- Reconcile [the runbook](../fork-sync.md) step 3, which still prescribes `vp check`,
  `vp run typecheck`, and `vp run build:desktop`, against gate 3, which forbids them.

One more for the same reader, found at gate 4: retiring
`fix(web): scope markdown actions to thread environment` did not return
`apps/web/src/components/ChatMarkdown.tsx` to upstream verbatim, because
`feat(issues): add GitHub Issues surface scoped to project windows` also edits a comment in it. The
project-windows and github-issues rebase scans should both name that file so the next tag sees the
overlap before a retirement decision assumes a single owner.
