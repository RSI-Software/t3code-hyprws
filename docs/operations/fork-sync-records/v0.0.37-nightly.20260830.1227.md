# Rehearsal: `hyprws` onto `v0.0.37-nightly.20260830.1227`

## Header

- Source: `origin/hyprws@525bb70f21ce07a9af8393633a18441fe2253fd4`
- Target: `v0.0.37-nightly.20260830.1227@2daff8c25adf701fddd062ae93b94cc57d420ec2`
- `expected_old`: `525bb70f21ce07a9af8393633a18441fe2253fd4`
- Rehearsal branch: `rehearse/v0.0.37-nightly.20260830.1227`
- Rebased head: `4772d60cb25863fd84c439e4bed89ddf7627b37c`
- Stack size: `137` fork commits
- Human sanity: donjor 2026-08-30

The shared base is `8dcb96314c976899e4df6951fb9af03131c2a46f`.

## Conflicts

| Fork commit                                                        | Domain          | File                                            | Hunks | Class      | Effort | What upstream changed                                                                                   | Resolution                                                                                                                      | Agent-safe?                                                                                            |
| ------------------------------------------------------------------ | --------------- | ----------------------------------------------- | ----: | ---------- | ------ | ------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| `a3ff40a8a3` `fix(web): retain completed thread timing`            | upstream-fixes  | `apps/web/src/components/Sidebar.logic.ts`      |     1 | mechanical | S      | Upstream removed the unreachable stage-badge wrapper and its `resolveServerBackedAppStageLabel` import. | Kept that deletion and added only the fork's still-used `formatRelativeTimeLabel` import; completed-turn timing remains intact. | yes — the two imports serve unrelated helpers and the focused tests cover the retained timing behavior |
| `9a4cc0ca6` `fix(web): make sidebar thread ordering direct (#246)` | thread-ordering | `apps/web/src/components/Sidebar.logic.test.ts` |     1 | mechanical | S      | Upstream removed the unreachable stage-badge tests and their `resolveSidebarStageBadgeLabel` import.    | Kept that deletion and retained only the ordering helper imports and tests introduced by the fork commit.                       | yes — the removed helper is unrelated and all ordering tests pass                                      |

No commit was skipped, retired, squashed, reordered, or reworded. The conflict resolutions were:

```diff
diff --git a/apps/web/src/components/Sidebar.logic.ts b/apps/web/src/components/Sidebar.logic.ts
@@
 import { isLatestTurnSettled } from "../session-logic";
-import { resolveServerBackedAppStageLabel } from "../branding.logic";
+import { formatRelativeTimeLabel } from "../timestampFormat";
```

```diff
diff --git a/apps/web/src/components/Sidebar.logic.test.ts b/apps/web/src/components/Sidebar.logic.test.ts
@@
   orderThreadsByProjectPreference,
   resolveProjectStatusIndicator,
-  resolveSidebarStageBadgeLabel,
+  resolveSidebarThreadOrderMarker,
+  resolveSidebarThreadSortOrderAfterDrop,
   resolveThreadRowClassName,
```

Replay evidence, read at `4772d60cb25863fd84c439e4bed89ddf7627b37c`:

- `git rev-list --count v0.0.37-nightly.20260830.1227..HEAD` is `137`, equal to the pre-rebase stack size.
- Every commit subject is byte-identical to its pre-rebase subject, in the same order.
- Every full message, including every `Fork-Domain` and `Fork-Tier` trailer, is byte-identical across all 137 commits.
- `git config rerere.enabled` is `true`; both conflict resolutions were reviewed against the upstream and fork hunks before continuing.

## Automerged overlap review

None. Orientation reported two overlapping files and classified both as hard conflicts; there were no automerged overlaps.

## Fork commits

| Fork commit and exact subject                                              | Domain          | Class summary    | Action                                                                                                                                                                                        | Grounding claim                                                                                                                    |
| -------------------------------------------------------------------------- | --------------- | ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| `304be701c4` `refactor(web): add physical project sidebar scope`           | project-windows | retire-candidate | keep — donjor confirmed upstream removed only the unrelated stage-badge helper; physical environment-and-project scope remains fork-only and its adjacent tests pass                          | n/a — no new product grounding claim; this target changed only an unreachable helper at the import seam                            |
| `a3ff40a8a3` `fix(web): retain completed thread timing`                    | upstream-fixes  | retire-candidate | keep — donjor confirmed that, despite `Fork-Upstreamable: yes`, upstream did not add completed-turn age, duration, or durable Done behavior; it only deleted the unrelated stage-badge helper | n/a — no new product grounding claim; the retained timing helpers and their integration were unchanged outside the resolved import |
| `3b5d1acac1` `feat(web): add manual sidebar thread ordering`               | thread-ordering | retire-candidate | keep — donjor confirmed upstream did not add durable per-project manual ordering and removed only the unrelated stage-badge helper/tests                                                      | n/a — no new product grounding claim; the target does not touch ordering behavior                                                  |
| `b84e6bc9b7` `feat(web): group sidebar threads into named sections (#205)` | thread-ordering | retire-candidate | keep — donjor confirmed upstream did not add named thread sections or group-drop behavior and removed only the unrelated stage-badge helper/tests                                             | n/a — no new product grounding claim; the target does not touch grouping behavior                                                  |
| `9a4cc0ca6` `fix(web): make sidebar thread ordering direct (#246)`         | thread-ordering | retire-candidate | keep — donjor confirmed `Fork-Upstreamable: no`; upstream did not add drop-to-manual ordering or its restore action, and only the obsolete stage-badge import conflicted                      | n/a — no new product grounding claim; the focused ordering tests prove the retained helper contract                                |

The other 132 rehearsed commits replayed with no conflict, no new decision, and no grounding claim.
Together with the five rows above, they reconcile with the header's stack size of 137.

## Silent seams

None. The targeted web typecheck emitted no diagnostics, and the adjacent test file passed all 131 tests.

## Verification

- `vp i`: **pass** — dependencies installed; the incidental lockfile refresh was restored before rehearsal.
- `node scripts/fork-preflight.ts`: **pass** — 6 preconditions met; mirror current at `2daff8c25adf`.
- `node scripts/fork-upstream-watch.ts`: **pass** — 6 open watches: 3 waiting, 2 uncited, 1 ready.
- `node scripts/fork-orient.ts --target v0.0.37-nightly.20260830.1227`: **pass** — target/source/shared base resolved; two hard conflicts and no automerged overlap reported.
- `git rebase v0.0.37-nightly.20260830.1227`: **pass** — 137/137 commits replayed with exactly two conflict files resolved.
- `vp run fork:scan --head origin/hyprws --target v0.0.37-nightly.20260830.1227`: **pass** — 12 domain scans cover every shared file; no gaps.
- `vp run fork:delta --check`: **pass** — 137 fork commits tagged.
- `vp run --filter @t3tools/web typecheck`: **pass** — no diagnostics.
- `vp test run apps/web/src/components/Sidebar.logic.test.ts`: **pass** — 1 file, 131 tests.

No assertion was weakened or skipped. Repo-wide checks were not substituted for the targeted set.

## Grounding

None. This target removes an unreachable stage-badge helper; it does not change the retained completed-timing, project-scope, grouping, or ordering implementations. The adjacent logic tests and targeted typecheck cover the resolved seams. Donjor explicitly accepted the structural evidence and directed the nightly to own browser-level proof.

Recommendation: land.
